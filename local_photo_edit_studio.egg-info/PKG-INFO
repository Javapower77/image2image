Metadata-Version: 2.4
Name: local-photo-edit-studio
Version: 0.1.0
Summary: Private local multi-model photo editing studio for NVIDIA H100
Requires-Python: <3.12,>=3.11
Description-Content-Type: text/markdown
Requires-Dist: accelerate<2,>=1.12
Requires-Dist: diffusers @ git+https://github.com/huggingface/diffusers.git
Requires-Dist: gradio<7,>=6.0
Requires-Dist: huggingface-hub[hf-transfer]<2,>=0.36
Requires-Dist: numpy<3,>=2.0
Requires-Dist: opencv-python-headless<5,>=4.10
Requires-Dist: Pillow<13,>=11
Requires-Dist: pydantic-settings<3,>=2.7
Requires-Dist: safetensors<1,>=0.5
Requires-Dist: sentencepiece<1,>=0.2
Requires-Dist: transformers<6,>=4.57
Provides-Extra: dev
Requires-Dist: pytest<10,>=8; extra == "dev"
Requires-Dist: ruff<1,>=0.9; extra == "dev"
Provides-Extra: restore
Requires-Dist: basicsr @ git+https://github.com/XPixelGroup/BasicSR.git ; extra == "restore"
Requires-Dist: facexlib>=0.3.0; extra == "restore"
Requires-Dist: gfpgan @ git+https://github.com/TencentARC/GFPGAN.git ; extra == "restore"
Requires-Dist: realesrgan>=0.3.0; extra == "restore"

# Local Photo Edit Studio

A private, local Gradio studio for high-quality instruction-based photo editing on an NVIDIA H100. It supports Qwen Image Edit 2511, FireRed Image Edit 1.1, and FLUX.2 [klein] 4B/9B through lazy-loaded Diffusers adapters.

## Features

- Background, clothing, object, scene, style, and localized edits.
- Source image plus multiple face/clothing/scene references.
- Identity-preservation prompt mode, deterministic seeds, CFG, true CFG, steps, strength, dimensions, masks, and batches.
- Optional local GFPGAN post-processing.
- One model in GPU memory at a time; explicit unload control.
- Local model cache and output storage. Prompts and source images are not written to logs or metadata.
- No cloud inference, API inference, telemetry, or share link by default.

## Requirements

- Linux Azure VM with an NVIDIA H100 and a recent NVIDIA driver.
- Python 3.11, Git, and at least 200 GB free disk if downloading every model.
- Hugging Face account/token for gated repositories. FLUX.2 [klein] 9B requires accepting its model terms and is non-commercial.

## Quick start

1. Run `bash scripts/setup.sh`.
2. Activate: `source .venv/bin/activate`.
3. Authenticate only if needed: `huggingface-cli login` (the token remains in the local Hugging Face cache).
4. Download selected models, for example: `python scripts/download_models.py qwen-2511 flux-klein-4b --restorers`.
5. Optionally install GFPGAN support: `pip install -e '.[restore]'`.
6. Start with `bash scripts/run.sh`.
7. From the VM itself, open `http://127.0.0.1:7860`. From a workstation, use SSH port forwarding: `ssh -L 7860:127.0.0.1:7860 USER@VM`.

To download all four model snapshots, run `python scripts/download_models.py`. Once downloaded, use `HF_HUB_OFFLINE=1 bash scripts/run.sh` for strict offline operation.

## Recommended editing workflow

1. Put the unmodified photo in **Source photo**.
2. Optionally add a tight, clean face crop first in **References**, followed by clothing/object references.
3. Describe only the intended change and explicitly name what must remain unchanged.
4. Start with the model defaults. Qwen/FireRed generally use 40 steps and true CFG 4; distilled FLUX Klein uses 4 steps and guidance 1.
5. Reuse the same seed while tuning the prompt.
6. Apply GFPGAN only when facial micro-detail needs correction; low weights reduce identity drift.

Example instruction:

> Replace only the jacket with the garment shown in image 2. Preserve the person’s exact face, body proportions, pose, hands, hair, lighting, camera angle, background, and skin texture.

## Important limitations

- Generic image-editing models cannot mathematically guarantee identity. A clean face crop and constrained prompt usually improve consistency.
- The mask is composited after generation in this release; it is not native latent inpainting.
- `strength`, dimensions, negative prompt, and batch count are passed only when supported by the selected pipeline API.
- FireRed supports 1–3 native inputs. This project intentionally does not use FireRed’s cloud-based recaption agent.
- Model APIs are recent and require the current Diffusers source build.
- GFPGAN can beautify or alter identity. Compare restored and original outputs.

## Safety, privacy, and licenses

This app is local, but local execution does not waive laws, consent requirements, or model licenses. It keeps model-native safeguards intact and includes narrow controls against high-risk sexual exploitation and non-consensual use. Do not use it for illegal content, deceptive impersonation, harassment, NCII, or edits of people without permission.

FLUX.2 [klein] 9B is gated, non-commercial, and requires filters or manual review under its terms. Qwen 2511, FireRed 1.1, and FLUX.2 [klein] 4B identify Apache-2.0 model licenses; verify current upstream terms before deployment.

See `docs/SETUP.md`, `docs/MODELS.md`, `docs/ARCHITECTURE.md`, `docs/USAGE.md`, and `docs/TROUBLESHOOTING.md`.
